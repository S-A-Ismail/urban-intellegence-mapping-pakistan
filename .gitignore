# download cache for rebuild_boundaries.py
_cache/

# local census overrides
census.json

# python
__pycache__/
*.pyc
.venv/
venv/

# excel lock files
~$*.xlsx

# app build (feature/urban-intelligence-mapping)
node_modules/
app/dist/
app/.vite/
.env
.env.*
!.env.example

# generated tiles — large, rebuilt by ETL, never committed
*.pmtiles
tiles/
